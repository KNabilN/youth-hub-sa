

## تحسين جدول إدارة الخدمات

### التغييرات

**`src/pages/admin/AdminServices.tsx`:**

1. **اقتصاص العنوان**: إضافة `truncate max-w-[120px]` على خلية العنوان مع `title` attribute لإظهار النص الكامل عند التمرير
2. **اقتصاص اسم مقدم الخدمة**: نفس المعاملة — `truncate max-w-[100px]`
3. **الصف كامل قابل للنقر**: تحويل `TableRow` لعنصر قابل للنقر بـ `onClick` + `cursor-pointer` ينقل لصفحة `/admin/services/${s.id}` — مع `e.stopPropagation()` على العناصر التفاعلية (الترتيب، المميز، تغيير الحالة، أزرار الإجراءات) حتى لا تتعارض

