

## خطة: إظهار رسالة شكر للمانحين بعد أول منحة

### التغيير

في `src/pages/Dashboard.tsx` — داخل دالة `DonorDashboard`:
- التحقق من `stats?.totalDonations > 0` (أي أن المانح قام بتحويل منحة واحدة على الأقل)
- عرض بانر شكر بتصميم جذاب (Card مع أيقونة قلب/يد) بنص باللهجة السعودية
- يظهر فقط عند وجود تبرع واحد أو أكثر، مع أنيميشن fade-in

### النص المقترح (لهجة سعودية)
> "الله يجزاك خير! دعمك وصل وأثره بيّن، شكراً لك من القلب على عطائك الكريم 🤍"

### الملفات المتأثرة

| الملف | التغيير |
|-------|---------|
| `src/pages/Dashboard.tsx` | بانر شكر في `DonorDashboard` |

