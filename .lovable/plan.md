

# إزالة حالة الطلب وعرض الطلبات المفتوحة فقط

## التغييرات المطلوبة

### 1. تعديل استعلام قاعدة البيانات (`src/hooks/useLandingStats.ts`)
- تغيير فلتر الطلبات من `neq("status", "draft")` إلى `eq("status", "open")` لعرض الطلبات المفتوحة فقط.

### 2. تعديل بطاقات الطلبات (`src/components/landing/LandingRequestsTable.tsx`)
- إزالة عنصر `Badge` الخاص بالحالة من كل بطاقة.
- إزالة `statusConfig` والأكواد المرتبطة به بالكامل لأنها لم تعد مستخدمة.
- تعديل تنسيق رأس البطاقة ليعرض الميزانية فقط بدون الحالة.

