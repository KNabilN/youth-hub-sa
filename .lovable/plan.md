

# توحيد لون خلفية قسم التواصل

## التغيير
في `src/pages/Index.tsx` السطر 164: تغيير خلفية قسم الهيدر من `bg-background` إلى `bg-muted/30` ليتطابق مع خلفية قسم النموذج تحته.

### التفاصيل التقنية
- تغيير class القسم من `bg-background py-16 px-4 text-center` إلى `bg-muted/30 py-16 px-4 text-center`
- هذا سيجعل القسمين (الهيدر + النموذج) بنفس اللون الرمادي الفاتح كقطعة واحدة متصلة
