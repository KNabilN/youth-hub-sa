
# عرض كامل معلومات مقدم الخدمة

## المشكلة
النص الحيوي (bio) مقيد بسطرين فقط (`line-clamp-2`) والاسم مقيد بـ `truncate`، فلا تظهر المعلومات كاملة.

## الحل
إزالة قيود الاقتصاص من الاسم والنص الحيوي.

## التغييرات

### `src/components/services/ServiceProviderCard.tsx`
- سطر 30: إزالة `truncate` من اسم مقدم الخدمة
- سطر 36: تغيير `line-clamp-2` إلى بدون حد (إزالتها) ليظهر النص كاملاً
