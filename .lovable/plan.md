

## خطة: إزالة سوق الخدمات وسلة المشتريات ومشترياتي من قائمة المانح

### التغييرات

#### 1. القائمة الجانبية (`src/components/AppSidebar.tsx`)
- حذف 3 عناصر من مصفوفة `donor`:
  - `سوق الخدمات` (`/marketplace`)
  - `سلة المشتريات` (`/cart`)
  - `مشترياتي` (`/donor-purchases`)

#### 2. لا حاجة لحذف الصفحات أو المسارات
- هذه الصفحات تُستخدم من أدوار أخرى (الجمعيات ومقدمي الخدمات) لذا تبقى كما هي
- فقط نزيل الوصول من قائمة المانح

| الملف | التغيير |
|-------|---------|
| `src/components/AppSidebar.tsx` | حذف 3 روابط من مصفوفة `donor` |

